<style>
	.tweet-wrapper{

	}
	.twitter-logo{

	}
	.user-name{

	}
	.tweet-body{

	}
</style>

<p> requested {{count}} results for {{username}} </p> 
<div class="tweets-wrapper">
	<div ng-repeat="tweet in tweets" class="tweet-wrapper">
		<img  class="twitter-logo" src="{{twitterlogo}}"/>
		<span class="user-name">@{{tweet.user.name}}</span>
		<span class="tweet-body">{{tweet.text}}</span>
	</div>
</div>